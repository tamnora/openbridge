# Dependencias
node_modules/

# Casa portable: configuracion y datos locales (por instalacion)
.openbridge/
# Layout viejo (pre `.openbridge/`); se migra solo, pero no se commitea.
config.json
app.json
folders.json
mode.txt
sync-state.json
.procs.json
.openbridge.pid
.bridge.pid
runtime.json
data/
logs/
*.log

# Empaquetados
*.tgz

# Sistema
.DS_Store
Thumbs.db

# Credenciales locales (nunca commitear)
.npmrc

# Artefactos temporales de Playwright MCP
.playwright-mcp/
