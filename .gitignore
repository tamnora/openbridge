# Dependencias
node_modules/

# Casa portable: configuracion y datos locales (por instalacion)
.openbridge/
# Layout viejo (pre `.openbridge/`); se migra solo, pero no se commitea.
config.json
app.json
folders.json
mode.txt
sync-state.json
.procs.json
.openbridge.pid
.bridge.pid
runtime.json
data/
logs/
*.log

# Empaquetados
*.tgz
*.zip

# Sistema
.DS_Store
Thumbs.db

# Credenciales locales (nunca commitear)
.npmrc
# Deploy del hub PHP: credenciales, manifiesto y backups locales
.deploy.env
.deploy-cache/
backups/

# Artefactos temporales de Playwright MCP
.playwright-mcp/

# Capturas de vista previa (locales)
preview/

# Hub PHP: `php/dist/` se genera con scripts/build-php-hub.mjs (no se commitea)
php/dist/
# El app.json de ejemplo SI se commitea (el patron `app.json` de arriba lo tapaba).
!php/app/.openbridge.example/app.json
